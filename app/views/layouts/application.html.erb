<!DOCTYPE html>
<html lang="fr">
  <head>
    <title><%= content_for(:title) || "SnapStock" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <%# --- CSS --- %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# --- SCRIPT (gardé pour le menu mobile, chargé en defer) --- %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">

    <%# --- NAVIGATION CLASSIQUE --- %>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom shadow sticky-top">
      <div class="container-fluid px-4">

        <%# LOGO %>
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center me-4 fw-bold" do %>
          <i class="fa-solid fa-layer-group me-2"></i> SnapStock
        <% end %>

        <% if Current.user %>
          <%# BOUTON MENU MOBILE %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">

              <%# 1. DASHBOARD %>
              <li class="nav-item">
                <%= link_to dashboard_path, class: "nav-link px-3 #{'active fw-bold' if current_page?(dashboard_path) || current_page?(root_path)}" do %>
                  <i class="fa-solid fa-gauge-high me-1"></i> Dashboard
                <% end %>
              </li>

              <%# 2. MAGASINS %>
              <li class="nav-item">
                <%= link_to stores_path, class: "nav-link px-3 #{'active fw-bold' if request.path.start_with?('/stores')}" do %>
                  <i class="fa-solid fa-store me-1"></i> Magasins
                <% end %>
              </li>

              <%# SEPARATEUR VISUEL (Optionnel) %>
              <li class="nav-item d-none d-lg-block border-start mx-2" style="height: 20px; opacity: 0.3;"></li>

              <%# 3. PRODUITS (Direct) %>
              <li class="nav-item">
                <%= link_to products_path, class: "nav-link px-3 #{'active fw-bold' if request.path.start_with?('/products')}" do %>
                  <i class="fa-solid fa-box-open me-1"></i> Produits
                <% end %>
              </li>

              <%# 4. RAYONS (Direct) %>
              <li class="nav-item">
                <%= link_to categories_path, class: "nav-link px-3 #{'active fw-bold' if request.path.start_with?('/categories')}" do %>
                  <i class="fa-solid fa-tags me-1"></i> Rayons
                <% end %>
              </li>

              <%# 5. ENSEIGNES (Direct) %>
              <li class="nav-item">
                <%= link_to brands_path, class: "nav-link px-3 #{'active fw-bold' if request.path.start_with?('/brands')}" do %>
                  <i class="fa-solid fa-building me-1"></i> Enseignes
                <% end %>
              </li>

            </ul>

            <%# ZONE UTILISATEUR (A droite) %>
            <div class="d-flex align-items-center gap-3 mt-3 mt-lg-0 pt-3 pt-lg-0 border-top border-lg-0 border-white-50">

              <%# Info User %>
              <div class="d-flex align-items-center text-white">
                <div class="bg-white text-primary rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 32px; height: 32px;">
                   <i class="fa-solid fa-user small"></i>
                </div>
                <span class="small fw-bold"><%= Current.user.display_name %></span>
              </div>

              <%# Bouton Déconnexion (Direct, pas de dropdown) %>
              <%= button_to session_path, method: :delete, class: "btn btn-sm btn-outline-light d-flex align-items-center", form_class: "d-inline" do %>
                <i class="fa-solid fa-power-off me-2"></i> Sortir
              <% end %>

            </div>
          </div>
        <% end %>
      </div>
    </nav>

    <%# --- FLASH MESSAGES --- %>
    <main class="container py-4">
      <div id="flash-messages" class="mb-4">
        <% flash.each do |type, message| %>
          <div class="alert <%= type == 'notice' ? 'alert-success' : 'alert-danger' %> alert-dismissible fade show shadow-sm" role="alert">
            <div class="d-flex align-items-center">
              <i class="fa-solid <%= type == 'notice' ? 'fa-circle-check' : 'fa-triangle-exclamation' %> me-2"></i>
              <span><%= message %></span>
            </div>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
      </div>

      <%= yield %>
    </main>

    <%# --- FOOTER --- %>
    <footer class="bg-light border-top py-4 mt-auto">
      <div class="container text-center text-muted">
        <small>&copy; <%= Time.current.year %> SnapStock</small>
      </div>
    </footer>

  </body>
</html>
