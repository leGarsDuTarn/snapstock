<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SnapStock | Retail Execution" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="SnapStock">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA Manifest %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts (Optionnel pour un look plus moderne) %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <%# Bootstrap 5 CSS %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <%# FontAwesome 6 %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <%# Custom Stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body { font-family: 'Inter', sans-serif; }
      .navbar-brand { letter-spacing: -0.5px; }
      .alert { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075); }
    </style>
  </head>

  <body class="bg-light">
    <%# Navigation %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center" do %>
          <i class="fa-solid fa-layer-group me-2"></i> SnapStock
        <% end %>

        <% if Current.user %>
          <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
              <li class="nav-item">
                <%= link_to stores_path, class: "nav-link" do %>
                  <i class="fa-solid fa-shop me-1"></i> Magasins
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to products_path, class: "nav-link" do %>
                  <i class="fa-solid fa-barcode me-1"></i> Catalogue
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to categories_path, class: "nav-link" do %>
                  <i class="fa-solid fa-layer-group me-1"></i> Rayons
                <% end %>
              </li>
            </ul>

            <div class="d-flex align-items-center mt-3 mt-lg-0">
              <span class="text-white-50 me-3 d-none d-lg-inline">
                <i class="fa-solid fa-user-circle me-1"></i> <%= Current.user.email_address %>
              </span>
              <%= button_to session_path, method: :delete, class: "btn btn-outline-light btn-sm w-100 w-lg-auto" do %>
                <i class="fa-solid fa-right-from-bracket me-1"></i> Déconnexion
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </nav>

    <%# Contenu Principal %>
    <main class="container">
      <%# Flash Messages (Alertes) %>
      <div id="flash">
        <% flash.each do |type, message| %>
          <div class="alert <%= type == 'notice' ? 'alert-success' : 'alert-danger' %> alert-dismissible fade show" role="alert">
            <i class="fa-solid <%= type == 'notice' ? 'fa-check-circle' : 'fa-exclamation-triangle' %> me-2"></i>
            <%= message %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
      </div>

      <%= yield %>
    </main>

    <footer class="container text-center text-muted py-5 mt-auto">
      <small>&copy; <%= Time.current.year %> SnapStock &middot; Relevés de parc linéaire</small>
    </footer>

    <%# Bootstrap 5 JS Bundle (nécessaire pour les menus déroulants et les alertes) %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
