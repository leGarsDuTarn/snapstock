<div class="row justify-content-center py-4">
  <div class="col-12 col-lg-6">
    <nav aria-label="breadcrumb" class="mb-3">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "Enseignes", brands_path, class: "text-decoration-none" %></li>
        <li class="breadcrumb-item"><%= link_to @stratum.brand.name, brand_path(@stratum.brand), class: "text-decoration-none" %></li>
        <li class="breadcrumb-item active">Modifier Strate</li>
      </ol>
    </nav>

    <div class="card shadow-sm border-0">
      <div class="card-body p-4 p-md-5">
        <div class="mb-4">
          <h1 class="h3 fw-800 mb-1 text-primary">Modifier la Strate</h1>
          <p class="text-muted small">Mise à jour de la strate <strong><%= @stratum.name %></strong>.</p>
        </div>

        <%= render "form", stratum: @stratum, brand: nil %>
      </div>
    </div>
  </div>
</div>
